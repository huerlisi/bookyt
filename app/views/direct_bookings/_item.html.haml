%tr
  %td{:style => "width: 10em"}= l item.value_date.to_date
  %td
    = link_to item.title, item
    - unless item.comments.blank?
      %hr{:style => "height: 1px; margin: 0"}
    = h item.comments
  %td.currency
    = h currency_fmt(item.accounted_amount(@direct_account))
    \&nbsp;CHF
