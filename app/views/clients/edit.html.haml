%h1 Editing client
- form_tag :action => 'update', :id => @client do
  = render :partial => 'form'
  = submit_tag 'Edit'
= link_to 'Show', :action => 'show', :id => @client
|
\#{link_to 'Back', :action => 'list'}
