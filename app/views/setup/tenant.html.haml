= boot_page_title

= simple_form_for @tenant, :url => 'setup/accounting' do |f|
  %h3= t('form.company.address')
  = f.fields_for :company do |p|
    = render 'has_vcards/vcards/form', :f => p, :full_vcard => true

  .form-actions
    = f.button :submit, t_action(:next), :class => 'btn-primary'
