= semantic_form_for @employment do |f|
  = f.semantic_errors
  = f.inputs do
    = f.input :employee, :as => :combobox
    = f.input :employer, :as => :combobox

  = render 'fields', :f => f

  = f.buttons do
    = f.commit_button
