.contextual
  = link_to t_action('edit'), edit_employer_path(@employer)
  = link_to t_action('index'), employers_path
%h1= t_title

= @employer.vcard.try(:full_name)

%h2= t_title :index, Employment

- @employments = @employer.employments.paginate(:page => params[:page])
= render 'employments/list'
