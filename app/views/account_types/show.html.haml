- for column in AccountType.content_columns
  %p
    %b
      = column.human_name
      \:
    = h @account_type.send(column.name)
= link_to 'Edit', :action => 'edit', :id => @account_type
|
\#{link_to 'Back', :action => 'list'}
