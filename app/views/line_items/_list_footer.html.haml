.span12.line_item_total
  = link_to t('new_position', scope: 'line_items'), [:new_line_item, resource.persisted? ? resource : resource.class.name.underscore.pluralize.to_sym], :remote => true, :class => 'btn'
  = link_to t('new_subtotal', scope: 'line_items'), polymorphic_url([:new_line_item, resource.persisted? ? resource : resource.class.name.underscore.pluralize.to_sym], :klass => 'SaldoLineItem'), :remote => true, :class => 'btn'
