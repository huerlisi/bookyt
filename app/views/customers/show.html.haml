.contextual
  = link_to t_action('edit'), edit_customer_path(@customer)
  = link_to t_action('index'), customers_path
%h1= t_title

= @customer.vcard.try(:full_name)

.contextual
  = link_to t_action('new', Invoice), new_invoice_path, :remote => true, :class => "icon icon-add"
%h2= t_title :index, Invoice

- @invoices = @customer.invoices.paginate(:page => params[:page])
= render 'invoices/list'
