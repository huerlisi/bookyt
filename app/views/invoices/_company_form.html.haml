- @invoice = resource_class.new
- @invoice.build_company
= fields_for @invoice do |invoice|
  = invoice.fields_for :company do |f|
    .row-fluid
      .span6
        %h3= f.inputs t('form.person.address')
          = render 'vcards/form', :f => f
