.contextual
  = link_to t_action('show'), @invoice
  = link_to t_action('index'), invoices_path
  = link_to t_action('delete'), @invoice, :confirm => t_confirm_delete(@invoice), :method => :delete
%h2= t_title

= render :partial => 'form'
