.contextual
  = link_to t_action('edit'), edit_employee_path(@employee)
  = link_to t_action('index'), employees_path
%h1= t_title

= @employee.vcard.try(:full_name)

.contextual
  = link_to t_action('new', Employment), new_employment_path, :remote => true, :class => "icon icon-add"
%h2= t_title :index, Employment

#new_employment
- @employments = @employee.employments.paginate(:page => params[:page])
= render 'employments/list'
