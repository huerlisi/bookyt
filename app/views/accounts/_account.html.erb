<tr>
  <td><%= account.code %></td>
  <td><%= link_to account.title, account %></td>
  <% if @date %>
    <td style="text-align: right"><%= currency_fmt(account.saldo(@date)) %></td>
  <% else %>
    <td style="text-align: right"><%= currency_fmt(account.saldo.abs) %></td>
  <% end %>
</tr>
