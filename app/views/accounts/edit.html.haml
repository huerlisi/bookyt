.contextual
  = link_to t_action('show'), @account
  = link_to t_action('index'), accounts_path
  = link_to t_action('delete'), @account, :confirm => t_confirm_delete(@account), :method => :delete
%h2= t_title

= render :partial => 'form'
