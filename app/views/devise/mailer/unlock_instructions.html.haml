%p
  = t('bookyt.devise.hello', :email => @resource.email)
%p
  = t('bookyt.devise.account_locked')
%p
  = t('bookyt.devise.click_to_unlock_account')
%p
  = link_to = t('bookyt.devise.unlock_my_account'), unlock_url(@resource, :unlock_token => @resource.unlock_token)
