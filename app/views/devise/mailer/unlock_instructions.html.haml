%p
  = t('devise.hello', :email => @resource.email)
%p
  = t('devise.account_locked')
%p
  = t('devise.click_to_unlock_account')
%p
  = link_to = t('devise.unlock_my_account'), unlock_url(@resource, :unlock_token => @resource.unlock_token)
